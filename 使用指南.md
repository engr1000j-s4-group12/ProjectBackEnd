# 龙宾楼智能导览后端使用指南

## 1. 当前版本能做什么

本项目在电脑上运行一个 FastAPI 后端，为后续的小智 ESP32-S3 终端提供：

- 将现场照片发送给 VLM，提取物品、标牌文字和场景特征；
- 使用视觉特征与地点地标库匹配当前位置；
- 返回最可能位置、候选位置、置信度和是否需要用户确认；
- 保留地点编号定位作为调试和故障兜底；
- 查询龙宾楼地点列表；
- 使用 Dijkstra 算法计算最短路线；
- 分别计算普通路线和无障碍路线；
- 返回中英文分段导航指令；
- 查询展品资料；
- 为 LLM/VLM 生成受展品资料约束的回答上下文。

当前地图和展品均为演示数据，不代表龙宾楼真实布局。团队完成实地测量后，需要修改 `data/` 中的数据文件。

视觉定位不会仅凭模型的一句话直接认定位置。VLM只负责提取客观特征，后端再与 `building_map.json` 中人工维护的视觉地标匹配。特征不足或多个地点相似时，接口会返回 `ambiguous` 或 `not_found`，终端必须要求用户确认或重新拍照。

## 2. 项目结构

```text
Project2/
├── server/
│   ├── app/
│   │   ├── main.py          # FastAPI 接口
│   │   ├── navigation.py    # 路径规划
│   │   ├── repository.py    # 地图与展品数据加载
│   │   ├── visual_localization.py # 视觉特征与地标匹配
│   │   ├── vlm.py           # OpenAI 兼容 VLM 客户端
│   │   └── schemas.py       # 请求和响应格式
│   ├── requirements.txt
│   └── requirements-dev.txt
├── data/
│   ├── building_map.json    # 地点、连线和导航指令
│   └── exhibits.json        # 展品资料
├── tests/
└── 使用指南.md
```

## 3. 环境准备

需要 Python 3.11 或更高版本。在项目根目录执行：

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r server/requirements-dev.txt
```

如果只运行服务、不运行测试，可以安装较小的运行依赖：

```bash
pip install -r server/requirements.txt
```

## 4. 启动服务器

在项目根目录执行：

```bash
source .venv/bin/activate
uvicorn server.app.main:app --host 0.0.0.0 --port 8000 --reload
```

浏览器打开以下地址：

- 健康检查：`http://127.0.0.1:8000/health`
- Swagger 接口页面：`http://127.0.0.1:8000/docs`
- OpenAPI 定义：`http://127.0.0.1:8000/openapi.json`

开发阶段可保留 `--reload`。正式演示时建议去掉，以避免文件变化导致服务自动重启：

```bash
uvicorn server.app.main:app --host 0.0.0.0 --port 8000
```

### 4.1 配置图片分析 VLM

`/api/v1/localize/image` 使用兼容 OpenAI Chat Completions 图片输入格式的 VLM。启动服务前设置：

```bash
export VLM_BASE_URL="https://你的VLM服务地址/v1"
export VLM_API_KEY="你的API密钥"
export VLM_MODEL="支持图片输入的模型名称"
export VLM_TIMEOUT_SECONDS="45"
```

未配置这些变量时，地图、路线和结构化特征定位仍可使用；直接上传图片会返回 HTTP 503。

不要把真实密钥写入代码、JSON 数据或 Git。后续可在项目根目录使用不提交的 `.env`，但当前启动命令不会自动读取 `.env`，仍需在 Shell 中导出变量。

## 5. 调用示例

### 5.1 健康检查

```bash
curl http://127.0.0.1:8000/health
```

预期结果：

```json
{
  "status": "ok",
  "service": "longbin-guide-server",
  "version": "0.2.0"
}
```

### 5.2 查询全部地点

```bash
curl http://127.0.0.1:8000/api/v1/places
```

### 5.3 上传照片进行视觉定位

配置 VLM 后，可以直接上传设备拍摄的图片：

```bash
curl -X POST http://127.0.0.1:8000/api/v1/localize/image \
  -F "image=@现场照片.jpg" \
  -F "floor_hint=2"
```

支持 JPEG、PNG 和 WebP，单张图片最大 5 MiB。`floor_hint` 可省略，但可靠的楼层信息能显著降低楼梯、电梯等相似场景的歧义。

成功且特征充分时返回：

```json
{
  "status": "matched",
  "node_id": "EXHIBIT-ROBOT",
  "confidence": 0.91,
  "needs_confirmation": false,
  "candidates": [
    {
      "node_id": "EXHIBIT-ROBOT",
      "name_zh": "机器人展品",
      "name_en": "Robot Exhibit",
      "floor": 2,
      "score": 0.91,
      "matched_features": ["白色机器人", "ROBOT-001"]
    }
  ],
  "evidence": {
    "objects": [],
    "recognized_texts": ["ROBOT-001"],
    "scene_description": "二楼走廊中的机器人展示区",
    "floor_hint": 2
  }
}
```

状态说明：

- `matched`：找到明显优于其他地点的候选；
- `ambiguous`：多个地点得分接近，必须让用户确认或重新拍照；
- `not_found`：没有足够视觉证据，不能开始导航；
- `needs_confirmation`：即使状态为 `matched`，低于高置信阈值时仍应询问用户。

### 5.4 使用已有VLM特征定位

如果图片已经由小智或其他 VLM 分析，不必再次上传图片，可以直接提交结构化特征：

```bash
curl -X POST http://127.0.0.1:8000/api/v1/localize/visual \
  -H "Content-Type: application/json" \
  -d '{
    "objects": [
      {"label":"银色电梯门","confidence":0.96},
      {"label":"电梯按钮面板","confidence":0.90}
    ],
    "recognized_texts":["2F"],
    "scene_description":"室内电梯厅",
    "floor_hint":2
  }'
```

推荐让小智已有的 VLM 输出该格式，再调用本接口，避免同一图片被分析两次。

### 5.5 使用地点编号定位（兜底）

```bash
curl -X POST http://127.0.0.1:8000/api/v1/localize \
  -H "Content-Type: application/json" \
  -d '{"marker_id":"LB-1F-ENTRANCE"}'
```

该接口也可配合二维码使用。二维码应保存准确地点编号，例如：

```text
LB-1F-ENTRANCE
LB-1F-LOBBY
LB-2F-CORRIDOR-A
EXHIBIT-ROBOT
```

### 5.6 规划普通路线

```bash
curl -X POST http://127.0.0.1:8000/api/v1/route \
  -H "Content-Type: application/json" \
  -d '{
    "from_location":"主入口",
    "to_location":"机器人",
    "language":"zh",
    "accessible_only":false
  }'
```

接口支持地点 ID、中文名、英文名和 `aliases` 中定义的别名。

### 5.7 规划无障碍路线

```bash
curl -X POST http://127.0.0.1:8000/api/v1/route \
  -H "Content-Type: application/json" \
  -d '{
    "from_location":"LB-1F-ENTRANCE",
    "to_location":"EXHIBIT-ROBOT",
    "language":"zh",
    "accessible_only":true
  }'
```

演示数据中，普通最短路线走楼梯，无障碍路线走电梯。

### 5.8 查询展品

```bash
curl http://127.0.0.1:8000/api/v1/exhibits/ROBOT-001
```

### 5.9 获取供 LLM 使用的展品上下文

```bash
curl -X POST http://127.0.0.1:8000/api/v1/exhibits/ROBOT-001/context \
  -H "Content-Type: application/json" \
  -d '{
    "question":"这个展品有什么意义？",
    "language":"zh"
  }'
```

该接口不会直接调用外部模型，而是返回展品事实、用户问题和回答约束。后续可将这些内容提交给选定的 LLM/VLM。

## 6. 让 ESP32 访问电脑

ESP32 与电脑必须连接同一个局域网。先在电脑上查询局域网地址：

```bash
hostname -I
```

假设电脑地址为 `192.168.1.20`，ESP32 应访问：

```text
http://192.168.1.20:8000
```

不要在 ESP32 中使用 `127.0.0.1`，该地址表示 ESP32 自身，不是电脑。

若 ESP32 无法连接：

1. 确认服务使用了 `--host 0.0.0.0`；
2. 确认电脑和 ESP32 位于同一网络；
3. 检查系统防火墙是否允许 TCP 8000 端口；
4. 先用同一网络下的手机访问 `http://电脑IP:8000/health`；
5. 演示现场尽量使用自有路由器或手机热点，避免校园网设备隔离。

## 7. 修改真实地图

地图文件为 `data/building_map.json`。

每个节点代表一个可以定位或导航到的位置：

```json
{
  "id": "LB-1F-ENTRANCE",
  "name_zh": "一楼主入口",
  "name_en": "First-floor Main Entrance",
  "floor": 1,
  "kind": "entrance",
  "aliases": ["入口", "main entrance"],
  "visual_landmarks": [
    {
      "aliases": ["龙宾楼门牌", "Longbin Building sign", "龙宾楼"],
      "weight": 4
    },
    {
      "aliases": ["玻璃双开门", "glass double doors"],
      "weight": 2
    }
  ]
}
```

`visual_landmarks` 是视觉定位的核心：

- `aliases`：同一物体、文字或建筑特征可能被 VLM 输出的中英文名称；
- `weight`：特征区分地点的能力，越独特权重越高；
- 楼层牌、房间号、展品编号等唯一文字应给予较高权重；
- “白墙”“门”“走廊”等常见物体不应单独作为高权重特征；
- 相邻地点尽量定义不同的特征组合，而不是只定义一个物体。

每条边代表两个节点之间可以通行：

```json
{
  "from": "LB-1F-ENTRANCE",
  "to": "LB-1F-LOBBY",
  "distance_m": 12,
  "bidirectional": true,
  "accessible": true,
  "instructions": {
    "forward": {
      "zh": "从主入口直行约12米，进入一楼大厅。",
      "en": "Go straight for about 12 meters into the lobby."
    },
    "reverse": {
      "zh": "穿过大厅直行约12米，到达主入口。",
      "en": "Cross the lobby and continue to the main entrance."
    }
  }
}
```

字段说明：

- `distance_m`：路径权重，单位为米；
- `bidirectional`：是否允许双向通行；
- `accessible`：轮椅等无障碍路线是否可使用；
- `forward`：从 `from` 前往 `to` 的指令；
- `reverse`：从 `to` 返回 `from` 的指令。

修改数据后应重启服务器。启动时会检查重复 ID、未知节点和非法距离。

## 8. 修改展品资料

展品文件为 `data/exhibits.json`。每个展品必须关联地图中的地点：

```json
{
  "id": "ROBOT-001",
  "location_id": "EXHIBIT-ROBOT",
  "name_zh": "机器人技术示范展品",
  "name_en": "Robotics Demonstration Exhibit",
  "facts_zh": ["中文事实一", "中文事实二"],
  "facts_en": ["English fact one", "English fact two"]
}
```

不要把未经确认的模型回答直接写入展品事实。正式数据应来自学院、展板、教师或其他可核实资料。

## 9. 运行测试

```bash
source .venv/bin/activate
pytest
```

当前测试覆盖：

- 数据文件加载和引用检查；
- 地点 ID 与别名解析；
- 普通最短路线；
- 无障碍电梯路线；
- 起点等于终点；
- HTTP 健康检查、定位、导航和展品接口；
- 视觉地标唯一匹配；
- 相似电梯场景的歧义检测；
- 楼层提示缩小候选范围；
- VLM 结构化 JSON 解析和图片类型校验；
- 未知地点的错误响应。

## 10. 与小智终端的下一阶段接口

建议后续在小智固件中增加两个设备端 MCP 工具：

```text
museum.visual_localize
museum.plan_route
```

推荐流程：

```text
用户提出导航请求
  → 小智 LLM 调用 visual_localize
  → ESP32 拍摄当前环境
  → 方案A：上传 /api/v1/localize/image，由本后端调用VLM
  → 方案B：小智VLM先提取特征，再调用 /api/v1/localize/visual
  → 后端返回 matched、ambiguous 或 not_found
  → 若需要确认，LLM向用户询问或要求重新拍照
  → LLM 确认目的地
  → 调用本后端 /api/v1/route
  → 小智直接播报 announcement，steps 仅供后台重新定位
  → ESP32 播放导航指令
```

展品讲解流程：

```text
ESP32 拍照
  → VLM 识别展品 ID
  → 查询 /api/v1/exhibits/{id}/context
  → LLM 仅依据返回 facts 作答
  → TTS 返回 ESP32
```

如果小智当前已经能够把图片提交给VLM并获得结构化结果，优先使用方案B，可以减少一次图片上传和模型调用。二维码可继续保留在入口、楼梯和电梯处作为演示故障兜底。

## 11. 现场数据采集要求

视觉定位能否工作主要取决于地标数据，而不是路径算法。正式测试前，每个导航节点至少采集：

1. 正面、左偏、右偏三个角度；
2. 近距离和远距离；
3. 白天和较暗环境；
4. 该位置稳定存在的物体、标牌文字和颜色特征；
5. 容易与该位置混淆的其他地点照片。

不要把行人、临时纸箱、可移动椅子等作为主要地标。先用实拍照片人工调用 `/api/v1/localize/image`，记录错误候选，再调整 `visual_landmarks` 的别名和权重。
